//@ mode: co2
//@ run-status: 0

#include <assert.h>

fn main() {
    assert((0 ? __builtin_unreachable() : 5) == 5);
    assert((1 ? 3 : std::process::exit(5)) == 3);
    int my_false = 0;
    assert((my_false ? __builtin_unreachable() : 5) == 5);
    assert((!my_false ? 3 : std::process::exit(5)) == 3);

    assert((0 ? 3 : std::process::exit(0)) == 7);
}
