//! Crate docs

/// nested module outer docs
mod nested;
mod ffi;

/// Docs for Counter type alias
pub type Counter = i32;

/// Docs for Point typedef.
/// Point has these fields:
/// * `x`: position in the x-axis
/// * `y`: position in the x-axis
typedef struct {
    int x;
    int y;
} Point;

typedef union {
    int whole;
    float frac;
} Number;

/// Docs for MAGIC static


/// With some intentional spaces
const int MAGIC = 7;

    /// Docs for MUTABLE static
int MUTABLE = 11;

/// Docs for
#[doc = "Rust-like"]
/// fn
pub fn public_value() -> i32 {
    return 2;
}

/// Docs for C-like
/// function
int c_abi_value(void) {
    return 9;
}

int *c_abi_with_args(int n, int* p) {
    return p;
}

void c_abi_with_args_unnamed(int, int*, int named, int) {
    return;
}

int c_abi_variadic(int n, int* p, ...) {
    return 9;
}

fn test_link_target() {}

/// Rust doc support for links [`test_link_target`] and [`nested::deeper::deep_value`].
fn test_link() {}

pub fn pub_fn() {}
pub(crate) fn pub_crate_fn() {}
pub(self) fn pub_self_fn() {}
fn no_pub_fn() {}

pub struct RustStruct {
    pub pub_field: i32,
    pub(crate) pub_crate_field: i32,
    pub(self) pub_self_field: i32,
    no_pub_field: i32,
}
