//@ mode: co2
//@ compile-fail

use std::option::Option;
use std::string::String;

fn f1(x: Option<String>) -> Option<usize> {
    return x.map(String::len);
           //^^^ error: a trait bound is not satisfied: `std::ops::FnOnce` for types [for<'a> fn(&'a std::string::String) -> usize {std::string::String::len}, (std::string::String,)]
}

fn main() {
    f1(Option::<String>::Some(String::new()));
}
