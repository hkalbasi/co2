# CO2 test harness

The code which runs the tests.

For usage, see [this](../tests/README.md).

Written by LLM, but don't rewrite it or do major changes in it, either by LLM or by hand,
since it is untested and may lose features in a rewrite.
